# appveyor.yml
image:
    - Visual Studio 2019

environment:
    matrix:
        - TOXENV: py312-pandas2
        - TOXENV: py313-pandas2
        - TOXENV: py314-pandas2
        - TOXENV: py315-pandas2
        - TOXENV: doc8

build: off

install:
    - py -m pip install --upgrade pip
    - py -m pip install tox

test_script:
    - py -m tox
